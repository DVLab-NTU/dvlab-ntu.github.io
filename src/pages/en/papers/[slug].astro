---
import Page from '../../../components/PaperPage.astro';
import { getCollection } from 'astro:content';
export async function getStaticPaths() {
  return (await getCollection('papers')).map(entry => ({ params: { slug: entry.slug }, props: { entry } }));
}
---
<Page lang="en" entry={Astro.props.entry} />
